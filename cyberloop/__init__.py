"""Cyberloop project package.

Hosts the project hooks consumed by ``flywheel run pattern``
plus any host-side helpers cyberloop patterns need.  The Decker
game itself lives in ``crates/`` (Rust) and is exposed to
Python via the ``decker_pyenv`` extension built from
``crates/pyenv``.
"""
