# Cyberloop workspace template.
#
# Declares the artifact contract every cyberloop pattern shares
# and the blocks the patterns reference by name.  Block bodies
# live under ``foundry/templates/blocks/<name>.yaml`` and are resolved
# at template-load time via the project's BlockRegistry.
artifacts:
  - name: game_engine
    kind: git
    repo: "."
    path: crates
  - name: checkpoint
    kind: copy
  - name: score
    kind: copy
  - name: bot
    kind: copy
  - name: decker_state
    kind: copy
  - name: cua_trace
    kind: copy
  - name: game_step
    kind: copy
  - name: gui_action_decider
    kind: copy
  - name: escalation_request
    kind: copy

blocks:
  - Train
  - Eval
  - EvalBot
  - ImproveBot
  - DeckerDesktop
  - CuaPlayAgent
  - StepThroughGui
  - GuiEscalationGate
  - ImproveGuiActionDecider
