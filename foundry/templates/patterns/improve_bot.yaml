name: improve_bot
lanes:
  - A
  - B
  - C
fixtures:
  bot: foundry/templates/assets/bots/baseline
steps:
  - name: improve_1
    cohort:
      min_successes: all
      foreach: lanes
      block: ImproveBot
  - name: improve_2
    cohort:
      min_successes: all
      foreach: lanes
      block: ImproveBot
